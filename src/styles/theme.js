import colors from "./colors";
import fonts from "./fonts";

export const theme = {
    colors: {
        primary: colors.primary,
        secondary: colors.secondary,
    },
    fonts: {
        main: fonts.primary,
    },
};
